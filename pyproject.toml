[project]
name = "myproject"
version = "0.1.0"
description = ""
authors = [
    {name = "abhijithEn",email = "abhiwalkerlouis@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ruff (>=0.16.2,<0.17.0)",
    "pytest (>=9.1.1,<10.0.0)",
    "bandit (>=1.9.4,<2.0.0)"
]

[tool.poetry]
packages = [{include = "myproject", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
